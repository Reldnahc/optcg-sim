{
  "name": "optcg-sim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "prepare": "husky",
    "format": "corepack pnpm exec prettier --write .",
    "format:check": "corepack pnpm exec prettier --check .",
    "lint": "corepack pnpm exec eslint . --max-warnings=0 --ext .js,.mjs,.cjs,.ts,.mts,.cts",
    "contracts": "corepack pnpm run contracts:compile && corepack pnpm run contracts:validate-effects && corepack pnpm run contracts:validate-db-schema && corepack pnpm run stories:validate && corepack pnpm run test:contracts",
    "contracts:compile": "corepack pnpm exec tsc -p contracts/tsconfig.json --noEmit",
    "contracts:validate-effects": "node --experimental-strip-types tools/validate-effect-dsl-fixtures.ts",
    "contracts:validate-db-schema": "node --experimental-strip-types tools/validate-database-schema.ts",
    "stories:validate": "node --experimental-strip-types tools/validate-stories.ts",
    "specs:generate-metadata": "node --experimental-strip-types tools/spec-metadata.ts generate",
    "specs:verify-metadata": "node --experimental-strip-types tools/spec-metadata.ts verify",
    "cli:boot": "corepack pnpm --filter @optcg/cli boot:summary",
    "packets:complete": "node --experimental-strip-types tools/build-agent-packet.ts complete",
    "packets:complete-many": "node --experimental-strip-types tools/build-agent-packet.ts complete-many",
    "packets:generate": "node --experimental-strip-types tools/build-agent-packet.ts generate",
    "packets:verify": "node --experimental-strip-types tools/build-agent-packet.ts verify-active",
    "typecheck": "corepack pnpm exec tsc -p packages/types/tsconfig.json --noEmit && corepack pnpm exec tsc -p packages/engine-core/tsconfig.json --noEmit && corepack pnpm exec tsc -p packages/cli/tsconfig.json --noEmit && corepack pnpm exec tsc -p tools/tsconfig.json --noEmit",
    "test": "corepack pnpm exec vitest run",
    "test:hidden-info": "corepack pnpm exec vitest run tests/hidden-info",
    "test:contracts": "corepack pnpm exec vitest run tests/contracts",
    "coverage": "corepack pnpm exec vitest run --coverage",
    "verify": "corepack pnpm run format:check && corepack pnpm run lint && corepack pnpm run typecheck && corepack pnpm run packets:verify && corepack pnpm run specs:verify-metadata && corepack pnpm run test && corepack pnpm run test:hidden-info && corepack pnpm run contracts"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^24.10.1",
    "@vitest/coverage-v8": "^4.1.5",
    "ajv": "^8.20.0",
    "eslint": "^9.39.1",
    "eslint-plugin-vitest": "^0.5.4",
    "globals": "^16.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "pgsql-ast-parser": "^12.0.2",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.3",
    "vitest": "^4.1.5"
  }
}
