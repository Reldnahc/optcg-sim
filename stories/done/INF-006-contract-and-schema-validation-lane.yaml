spec_version: v6
spec_package_name: optcg-md-specs-v6
story_schema_version: 1.0.0
id: INF-006
epic_id: KICK-001
title: Add contract and schema validation lane
type: tooling
area: contracts
primary_concern: verification
priority: critical
status: replaced
summary: >
  This broad candidate was replaced during normalization. Story-schema validation and the
  root contracts lane already landed in the repo foundation, and the remaining contract
  validation work is split into smaller approved stories so each contract concern can be
  reviewed independently.
story_boundary: >
  Own contract and schema validation commands only. Do not add general CI workflow
  orchestration, hidden-information regression logic, or gameplay tests beyond contract-facing fixtures.
allowed_touch_points:
  - contracts/**
  - package.json
  - tools/**
  - scripts/**
  - fixtures/**
  - stories/**
spec_refs:
  - 11-testing-quality.s021 (v6 contract validation)
  - 22-v6-implementation-tightening.s019 (Spec-driven delivery artifacts are now defined)
  - 23-repo-tooling-and-enforcement.s010 (Test tooling requirements)
  - 23-repo-tooling-and-enforcement.s011 (Contract and fixture validation)
  - 23-repo-tooling-and-enforcement.s016 (CI merge gates)
scope:
  - historical record of the oversized candidate before decomposition
non_scope:
  - implementation work
  - review authority over the replacement child stories
dependencies:
  - INF-001
  - INF-004
acceptance_criteria:
  - replacement child stories exist for canonical types, effect DSL fixtures, database schema validation, and root lane aggregation
required_tests:
  - none; replacement tracking artifact only
repo_rules:
  - replacement stories must stay concern-scoped and independently reviewable
ambiguity_policy: fail_and_escalate
