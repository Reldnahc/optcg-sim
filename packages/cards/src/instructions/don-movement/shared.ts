import type { SelectionId } from "@optcg/types";

export const donAttachSelection = "donSelection:attach" as SelectionId;
export const donAttachTarget = "targetSelection:attach-don";
export const donActivationTarget = "targetSelection:set-don-active";
